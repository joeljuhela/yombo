version: "3.3"

services:
  backend:
    build:
      ./server
    volumes:
      - ./server/db/:/yombo_server/db
    restart: unless-stopped
  nginx:
    build:
      ./frontend
    ports:
      - "8000:80"
    restart: unless-stopped
volumes:
  db:
